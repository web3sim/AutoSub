{
  "name": "autosub-scripts",
  "version": "1.0.0",
  "description": "AutoSub deployment and utility scripts",
  "type": "module",
  "scripts": {
    "deploy": "node deploy.js",
    "build": "tsc deploy.ts --outDir ./build --target ES2020 --module ESNext"
  },
  "devDependencies": {
    "@types/node": "^20.19.9",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@massalabs/massa-web3": "^4.0.0",
    "dotenv": "^16.0.0"
  }
}
